{
  "name": "nodejs-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.js --bundle --platform=node --external:express --external:pino --outfile=dist/bundle.js",
    "start": "node dist/bundle.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.2",
    "pino": "^9.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
